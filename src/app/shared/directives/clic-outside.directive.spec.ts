import { ClicOutsideDirective } from './click-outside.directive';

describe('ClicOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new ClicOutsideDirective();
    expect(directive).toBeTruthy();
  });
});
