import { CopyonclickDirective } from './copy-on-click.directive';

describe('CopyonclickDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyonclickDirective();
    expect(directive).toBeTruthy();
  });
});
