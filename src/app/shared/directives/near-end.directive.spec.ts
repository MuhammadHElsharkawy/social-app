import { NearEndDirective } from './near-end.directive';

describe('NearEndDirective', () => {
  it('should create an instance', () => {
    const directive = new NearEndDirective();
    expect(directive).toBeTruthy();
  });
});
